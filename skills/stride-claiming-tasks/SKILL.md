---
name: stride-claiming-tasks
description: Claim Stride tasks with proper hook execution, prerequisite verification, and before_doing result capture. Invoke before calling GET /api/tasks/next or POST /api/tasks/claim.
license: MIT
compatibility: opencode
metadata:
  category: stride-workflow
  version: "1.0"
---

<!-- Skill content will be populated in a subsequent task -->
